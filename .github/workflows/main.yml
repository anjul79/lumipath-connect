name: 🚀 Deploy to Hostinger (via FTP IP)

on:
  push:
    branches:
      - main   # change to "master" if your repo uses master branch

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Checkout Repository
        uses: actions/checkout@v3

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18   # pick version matching your project (16, 18, or 20)

      - name: 📦 Install Dependencies
        run: npm install

      - name: 🏗️ Build Project
        run: npm run build
        
      - name: 📤 Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: 145.79.210.130   # 🔹 Replace with your actual Hostinger FTP IP
          username: u205157316
          password: Nonu27@top
          port: 21
          server-dir: /public_html/
